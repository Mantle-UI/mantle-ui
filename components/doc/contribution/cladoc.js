import { DocSectionText } from '@/components/doc/common/docsectiontext';

export function CLADoc(props) {
    return (
        <DocSectionText {...props}>
            <p>Mantle UI does not currently require a separate Contributor License Agreement. Contributions are handled under the repository contribution policy and the project license.</p>
        </DocSectionText>
    );
}
